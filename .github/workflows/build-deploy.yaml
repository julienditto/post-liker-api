name: Build and Deploy Code

on: [push, pull_request]

jobs: 
  job1:
    env:
      SQLALCHEMY_DATABASE_URL='postgresql://postgres:Computer2012@localhost/fastapi'
      SECRET_KEY="4107274fbf4da56f9e7900e16c4a10031930de991e0b9c5df13bdb2505ada099"
      ALGOIRTHM="HS256"
      ACCESS_TOKEN_EXPIRE_MINUTES=300
      POSTGRES_DB="fastapi"
      POSTGRES_PASSWORD="Computer2012"
    runs-on: ubuntu-latest
    steps:
      - name: pulling git repo
        uses: actions/checkout@v2
      - name: say hi to Julien
        run: echo "hello Julien"
      - name: install python-3.9
        uses: actions/setup-python@v2
        with:
          python-version: "3.9"
      - name: update pip
        run: python -m pip install --upgrade pip
      - name: install all dependencies
        run: pip install -r requirements.txt
      - name: test with pytest
        run: |
          pip install pytest
          pytest